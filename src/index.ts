/** Host half: this plugin only adds a Web session-menu action. */
export const name = 'dsh-copy-session-ref'

/** No Host behavior. Client injects the copy-reference menu item. */
export function apply(): void {}
